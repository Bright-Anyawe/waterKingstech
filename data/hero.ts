/**
 * Hero background video. To replace it, drop new files in /public/videos and
 * update these paths. The poster should be the video's first frame so the
 * switch from poster to playback is seamless.
 */
export const heroMedia = {
  video: "/videos/hero.mp4",
  poster: "/videos/hero-poster.jpg",
} as const;
